import { AudioEvidenceUploader } from "@/features/audio-evidence"

/**
 * Audio Evidence page — upload recordings for Speechmatics transcription and
 * AI/ML API compliance-signal extraction.
 */
export default function AudioEvidencePage() {
  return <AudioEvidenceUploader />
}
