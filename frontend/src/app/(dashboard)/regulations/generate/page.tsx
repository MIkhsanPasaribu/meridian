import { RegulationsPage } from "@/features/regulations"

/**
 * Report generation page — same as regulations page with generation focus.
 */
export default function GenerateReportRoute() {
  return <RegulationsPage />
}
